{
  "name": "auco-familia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-runtime-config.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "10.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}

